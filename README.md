# Material ui project
- vite
- vitest
- npm install @mui/material @emotion/react @emotion/styled
- npm install vitest
- npm install jsdom
- npm install --save-dev @testing-library/react
## en vite.config 
-  test: {
    environment: 'jsdom',
    globals: true
  }